---
all:
  children:
    machines:
      hosts:
        192.168.1.104:
    local:
      vars:
        ansible_connection: local
      hosts:
        localhost
    server:
      vars:
        ansible_python_interpreter: /usr/bin/python3
      hosts:
        10.0.30.3:
